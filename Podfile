# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'memory-card' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for memory-card
  pod 'pop', '~> 1.0'

  target 'memory-cardTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'memory-cardUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
